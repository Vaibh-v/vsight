import { signIn, signOut, useSession } from "next-auth/react";
export default function Connect({provider="google"}:{provider?:string}){const {data:session,status}=useSession();const loading=status==="loading";return(<div className="p-4 border rounded-lg"><h3 className="font-medium mb-2">Auth ({provider})</h3>{loading?(<p>Loading session…</p>):session?(<div className="space-y-2"><p className="text-sm">Signed in as <span className="font-medium">{session.user?.email}</span></p><button className="px-3 py-2 rounded bg-gray-200 hover:bg-gray-300" onClick={()=>signOut()}>Sign out</button></div>):(<button className="px-3 py-2 rounded bg-black text-white hover:bg-gray-800" onClick={()=>signIn(provider)}>Sign in with Google</button>)}</div>);}